/*import React, { useEffect, useContext } from 'react'
import Comment from './Comment.js'
// import { CommentContext } from "../context/CommentProvider.js"



export default function CommentList(props) {
    
    const { comments } = props
    // const { getAllComments } = useContext(CommentContext)

 /*   useEffect(() => {
        getAllComments()
    }, [])


    return (
        <div className="comment-list">
            <h1>hii</h1>
            {comments.map(comment => <Comment {...comment}  issueId={comment.issueId} key={comment._id}/>)}
        </div>
    
)


// comments v comment?


//key={comment.issue._id}

// userId={issue.user} key={issue._id}*/